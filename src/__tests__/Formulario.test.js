import Formulario from "../Componentes/Formulario/Formulario.js";
import { render, screen } from '@testing-library/react';

//deve fazer algo
it('deve redirecionar o usuário para a tela de pedidos quando ocorre uma autenticação de sucesso', () => {
    
})